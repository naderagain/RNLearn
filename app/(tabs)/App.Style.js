import { StyleSheet, View, Text } from 'react-native'

export const s = StyleSheet.create({
    title: {
        color: "red",
        marginTop: 50,
        marginLeft: "40%"

    },

    square: {
        height: 100,
        backgroundColor: "green",
        width: 100,
    },
})